# -*- mode: snippet -*-
# name: matrixfor
# key: matfor
# --
for (int ${2:rowindex} = 0; $2 < ${1:matrix}.rows(); $2++) {
    for (int ${3:colindex} = 0; $3 < $1.cols(); $3++) {
        ${0:statement};
    }
}